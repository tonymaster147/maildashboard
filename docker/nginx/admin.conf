server {
    listen 80;
    root /usr/share/nginx/html/admin;
    index index.html;

    location /admin {
        try_files $uri $uri/ /admin/index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
